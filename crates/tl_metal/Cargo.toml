[package]
name = "tl_metal"
version = "0.1.0"
edition = "2021"
description = "Metal GPU tensor library for TL"
license = "MIT"

[dependencies]
metal = "0.29"
objc = "0.2"
block = "0.1"

[dev-dependencies]
